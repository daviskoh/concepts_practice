// Playground - noun: a place where people can play

import Cocoa

let 🐶🐮 = "dogcow"
println(🐶🐮)

// swift always chooses Double when inferring floating-point numbers
let pi = 3.14159

// combining integer & float will infer Double
let anotherPi = 3 + 0.14159

// can specify exponent in decmial numbers
let bigThang = 1.25e2

// numeric literals can be formatted w/ underscores
let oneMillion = 1_000_000

/*
 * Numeric Type Conversion
 */
// T(v) converts value v to type T
