var module1 = require('../lib/modules');

describe('module', function() {
  it('is an object', function() {
    // console.log(module);
    expect(typeof module === 'object');
  });
});

describe('module1', function() {
  it('is an function', function() {
    // console.log(module1);
    expect(typeof module1 === 'function');
    expect(module1()).toBe('module.exports');
  });
});