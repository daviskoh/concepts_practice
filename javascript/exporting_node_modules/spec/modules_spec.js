var modules1 = require('../lib/modules');

describe('module', function() {
  it('is an object', function() {
    // console.log(module);
    expect(typeof module === 'object');
  });
});

describe('modules1', function() {
  it('is an function', function() {
    // console.log(modules1);
    expect(typeof modules1 === 'function');
  });
});